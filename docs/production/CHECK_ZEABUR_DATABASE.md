# 检查Zeabur上的数据库状态

## 📡 方法1：通过健康检查API（最简单）

### 步骤1：获取应用URL

在Zeabur项目页面找到您的应用URL，格式：
```
https://your-app-name.zeabur.app
```

### 步骤2：访问健康检查端点

在浏览器中打开：
```
https://your-app-name.zeabur.app/health
```

### 预期响应

如果数据库正常，应该看到：
```json
{
  "status": "healthy",
  "timestamp": "2025-12-15T02:08:00.000Z",
  "uptime_seconds": 3600,
  "checks": {
    "database": {
      "status": "healthy",
      "message": "Database connection OK",
      "response_time_ms": 5.23
    },
    "api_config": {
      "status": "healthy"
    },
    "resources": {
      "status": "healthy"
    }
  }
}
```

### 如果数据库有问题

可能看到：
```json
{
  "status": "unhealthy",
  "checks": {
    "database": {
      "status": "unhealthy",
      "message": "Database connection failed: ...",
      "error": "..."
    }
  }
}
```

---

## 📡 方法2：通过部署状态API（详细信息）

### 访问部署状态端点

在浏览器中打开：
```
https://your-app-name.zeabur.app/admin/deployment/status
```

### 预期响应

```json
{
  "success": true,
  "status": {
    "database": {
      "connected": true,
      "status": "healthy"
    },
    "pages": {
      "total": 12,
      "enabled": 12,
      "pages": [...]
    },
    "token": {
      "default_token_configured": true,
      "token_type": "USER"
    }
  }
}
```

---

## 📡 方法3：使用curl命令（如果有命令行）

```bash
# 健康检查
curl https://your-app-name.zeabur.app/health

# 部署状态
curl https://your-app-name.zeabur.app/admin/deployment/status
```

---

## 🔍 数据库状态说明

### 正常状态

- `"status": "healthy"` - 数据库连接正常
- `"connected": true` - 数据库已连接
- `"response_time_ms"` - 响应时间正常（通常 < 100ms）

### 异常状态

- `"status": "unhealthy"` - 数据库连接失败
- `"connected": false` - 数据库未连接
- 有 `"error"` 字段 - 显示具体错误信息

---

## 🆘 如果数据库连接失败

### 常见原因和解决方案

#### 问题1：PostgreSQL服务未启动

**症状：** 连接超时或拒绝连接

**解决：**
1. 在Zeabur项目页面检查PostgreSQL服务状态
2. 确认服务已启动（状态为 "Running"）
3. 如果未启动，启动PostgreSQL服务

#### 问题2：DATABASE_URL未设置

**症状：** 错误信息显示 "DATABASE_URL not configured"

**解决：**
1. 确认已添加PostgreSQL服务到项目
2. Zeabur会自动设置 `DATABASE_URL` 环境变量
3. 如果未自动设置，检查服务连接

#### 问题3：数据库迁移未运行

**症状：** 表不存在错误

**解决：**
1. 数据库迁移应该在 `postDeploy` 中自动运行
2. 如果未运行，可以通过API触发（如果实现了）
3. 或等待服务重启后自动运行

#### 问题4：连接数过多

**症状：** "too many connections" 错误

**解决：**
1. 检查连接池配置
2. 等待连接释放
3. 重启服务

---

## 📊 数据库性能检查

### 检查响应时间

健康检查API会返回数据库响应时间：
```json
{
  "checks": {
    "database": {
      "response_time_ms": 5.23
    }
  }
}
```

**正常范围：**
- ✅ < 50ms - 优秀
- ✅ 50-100ms - 良好
- ⚠️ 100-500ms - 可接受
- ❌ > 500ms - 需要优化

### 检查连接池状态

如果实现了连接池监控，可以通过API查看：
- 连接池大小
- 已使用连接数
- 可用连接数

---

## ✅ 快速检查清单

- [ ] 访问 `/health` 端点
- [ ] 确认 `database.status` 为 `"healthy"`
- [ ] 确认 `database.connected` 为 `true`
- [ ] 检查响应时间是否正常
- [ ] 查看是否有错误信息

---

## 📚 相关文档

- [健康检查说明](../monitoring/HEALTH_CHECK.md)
- [部署后操作步骤](POST_DEPLOYMENT_STEPS.md)
- [故障排查指南](TROUBLESHOOTING_BROWSER.md)




