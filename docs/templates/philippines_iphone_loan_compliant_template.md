# 菲律宾苹果贷合规聊天机器人模板使用指南

## 📋 概述

这是一个符合 **Meta Non-Deceptive Policy** 规范的菲律宾苹果贷聊天机器人配置模板。该模板确保所有文案和流程都符合Facebook/Meta的广告和消息政策，避免因违规导致的账户封禁或投诉。

## ⚠️ 安全引流架构（核心原则）

**Facebook不"直接拉你去TG"，而是"用户主动选择"**

### 安全链路设计

```
Facebook 广告 / Page
    ↓
Messenger 自动回复（三连问，只筛选，不引流）
    ↓
用户回复 YES / 1 2 3（明确表示"想了解"）
    ↓
发送「可选外部渠道」说明（强调optional）
    ↓
用户主动点击 TG Bot（用户主动选择）
```

### 三步骤安全流程

1. **第一步（step1_reply）**：只筛选，不引流
   - ❌ 不包含TG链接
   - ❌ 不包含"去TG"的暗示
   - ✅ 只是筛选用户是否符合条件

2. **第二步（step2_reply_after_yes）**：说明可选渠道
   - ✅ 感谢确认
   - ✅ 说明可以在这里继续
   - ✅ 提到"可选"的Telegram助手
   - ✅ 强调"This is optional"
   - ⏱️ 等3-5秒后再发下一条

3. **第三步（step3_safe_drainage_template）**：用户主动选择
   - ✅ "Telegram is used only for..."（明确用途）
   - ✅ "Click here only if you wish to proceed"（用户主动选择）
   - ✅ "If you prefer, we can continue here as well"（提供选择）
   - ✅ 必须包含关键词：optional, if you wish, continue here as well

### 安全关键词清单

**必须使用**：
- ✅ "optional"
- ✅ "if you wish"
- ✅ "continue here as well"
- ✅ "you may choose"
- ✅ "This is optional"
- ✅ "whichever you prefer"
- ✅ "your choice"

**避免使用**（不安全，容易封号）：
- ❌ "must join" / "必须加入"
- ❌ "required to" / "需要"
- ❌ "need to go to" / "需要去"
- ❌ "join our Telegram to continue" / "加入TG才能继续"
- ❌ 任何强制性语言

## ✅ 合规要点

本模板确保以下合规要求：

1. **✅ 不承诺下款**：文案中不包含任何承诺性语言
2. **✅ 不收费、不留诱导性文案**：使用中性、客观的确认性问题
3. **✅ 是"确认条件"，不是"推销"**：三个问题都是确认用户是否符合条件
4. **✅ 明确"可忽略" → 降低投诉**：明确告知用户"可以忽略此消息"
5. **✅ 安全引流架构 → 低封号风险**：
   - 第一步不包含TG链接
   - 第二步强调"optional"
   - 第三步强调"if you wish"和"continue here as well"
   - 用户主动选择，而非强制引导
6. **✅ 符合 Meta 的 Non-Deceptive Policy**：
   - 不包含误导性信息
   - 明确告知用户可以忽略
   - 使用客观的确认性问题
   - 不承诺任何结果
   - 不强制跳转外部平台

## 🔄 流程设计

```
用户点击广告/页面
    ↓
Greeting Text（一次，首次问候）
包含三连问确认：
  1️⃣ Are you currently in the Philippines?
  2️⃣ Are you 18 years old or above?
  3️⃣ Would you like to learn more about the loan application process?
    ↓
用户回复 YES / 1 2 3 / 其他
    ↓
Webhook 判断：
  ✅ 全部YES → 继续自动流程（二级自动）
  ❌ 其他情况 → 转人工
```

## 📝 配置步骤

### 步骤1：复制配置到主配置文件

将 `config/config_philippines_iphone_loan_compliant.yaml` 中的配置内容合并到 `config/config.yaml` 中，或直接引用该文件。

### 步骤2：设置Telegram链接

在配置文件中找到 `telegram_link` 字段，替换为实际的Telegram群组链接：

```yaml
three_step_flow:
  telegram_link: "t.me/your_actual_group"  # ⚠️ 替换为实际链接
```

### 步骤3：设置Facebook Greeting Message

在Facebook页面设置中，将以下内容设置为 **Greeting Message**：

```
Hi 👋 Thanks for your message.

To check if we can assist you, please confirm:
1️⃣ Are you currently in the Philippines?
2️⃣ Are you 18 years old or above?
3️⃣ Would you like to learn more about the loan application process?

You may ignore this message if not interested.
```

**设置方法**：
- 访问 Facebook 页面设置
- 找到 "Messenger" 或 "消息" 选项
- 设置 "问候语"（Greeting Message）
- 粘贴上述文案

### 步骤4：启用配置

确保在 `config/config.yaml` 中启用三步流程：

```yaml
three_step_flow:
  enabled: true
```

### 步骤5：测试流程

1. 使用测试账号向页面发送消息
2. 确认收到包含三连问的Greeting Text
3. 测试回复"YES"、"1 2 3"等格式
4. 确认路由逻辑正常工作

## 🔍 路由判断逻辑

### 全部YES → 继续自动流程（二级自动）

当用户回复满足以下条件时，系统将标记为"qualified"，继续自动流程：

- 回复包含 `yes_keywords` 中的关键词（如 "yes", "y", "ok"）
- 回复包含数字格式（如 "1 2 3", "123"）

系统将发送Telegram群组链接，引导用户进入二级自动流程。

**当前实现状态**：✅ 已实现  
系统会自动检测用户回复YES，发送Telegram链接，然后继续AI自动回复流程。

### 其他情况 → 转人工

当用户回复不满足"全部YES"条件时，系统将：

- 发送 `step2_reply` 提示用户确认
- 标记为 `needs_review`（需要代码扩展）
- 发送到Telegram审核群组
- 由人工客服接管对话

**当前实现状态**：⚠️ 部分实现  
- ✅ 已实现：发送step2_reply提示
- ⚠️ 待扩展：自动标记为needs_review并转人工

**扩展建议**：  
如需实现自动转人工功能，可以在 `src/ai/three_step_flow.py` 的 `get_reply_for_step` 方法中，当检测到用户回复不符合"全部YES"条件且已多次提示时，设置 `conversation.should_review = True`，系统会自动发送到Telegram审核群组。

## 📊 配置项说明

### three_step_flow 配置

| 配置项 | 说明 | 必需 |
|--------|------|------|
| `enabled` | 是否启用三步流程 | ✅ |
| `step1_reply` | 第一步回复（Greeting Text） | ✅ |
| `step2_reply` | 第二步回复（用户未回复YES时） | ✅ |
| `step3_reply_template` | 第三步回复模板（全部YES后） | ✅ |
| `telegram_link` | Telegram群组链接 | ✅ |
| `routing_rules` | 路由判断规则 | ✅ |

### routing_rules 配置

| 配置项 | 说明 | 默认值 |
|--------|------|--------|
| `all_yes_action` | 全部YES时的动作 | `continue_auto` |
| `other_action` | 其他情况时的动作 | `transfer_to_human` |
| `yes_keywords` | YES识别关键词列表 | `["yes", "y", "1", "ok", ...]` |
| `number_keywords` | 数字格式关键词 | `["1 2 3", "123", ...]` |

## 💡 使用示例

### 示例1：用户回复"YES"

```
用户: YES
系统: Great! Thank you for confirming.
      To keep information organized and updated, we share details via Telegram.
      👉 Join here: t.me/your_group
      我们的团队将协助您完成贷款申请流程。
```

### 示例2：用户回复"1 2 3"

```
用户: 1 2 3
系统: Great! Thank you for confirming.
      To keep information organized and updated, we share details via Telegram.
      👉 Join here: t.me/your_group
      我们的团队将协助您完成贷款申请流程。
```

### 示例3：用户回复其他内容

```
用户: Maybe
系统: Thank you for your response.
      To proceed, please confirm all three questions:
      1️⃣ Are you currently in the Philippines?
      2️⃣ Are you 18 years old or above?
      3️⃣ Would you like to learn more about the loan application process?
      Reply YES if you meet all conditions, or contact us for assistance.
```

**同时**：该消息会被标记为 `needs_review`，发送到Telegram审核群组，由人工客服接管。

## ⚠️ 注意事项

### 1. 合规性要求

- **必须使用英语**：所有文案必须使用英语，符合Meta规范
- **不得修改核心文案**：三连问的文案是经过合规审核的，不要随意修改
- **不得添加承诺性语言**：不要添加"保证下款"、"100%通过"等承诺性语言
- **保持"可忽略"选项**：必须保留"You may ignore this message if not interested."

### 2. 技术限制

- **24小时消息窗口**：Facebook限制页面只能在用户主动发送消息后24小时内回复
- **Greeting Message限制**：Facebook Greeting Message最多160字符
- **消息发送频率**：避免短时间内发送多条消息

### 3. 最佳实践

- **定期监控**：定期检查用户回复，优化路由规则
- **A/B测试**：可以测试不同的文案变体（但必须保持合规）
- **人工审核**：对于不确定的情况，优先转人工处理
- **数据分析**：统计用户回复模式，优化判断逻辑

## 🔧 自定义配置

### 修改YES识别关键词

如果需要添加或修改YES识别关键词，编辑 `routing_rules.yes_keywords`：

```yaml
routing_rules:
  yes_keywords:
    - "yes"
    - "y"
    - "1"
    - "ok"
    - "sure"
    - "your_custom_keyword"  # 添加自定义关键词
```

### 修改Telegram链接

```yaml
three_step_flow:
  telegram_link: "t.me/your_actual_group"  # 替换为实际链接
```

### 自定义回复文案

可以自定义 `step2_reply` 和 `step3_reply_template`，但必须：

- 保持合规性（不承诺、不诱导）
- 使用英语
- 保持友好的语调

## 📚 相关文档

- [三步流Messenger设置指南](../guides/三步流Messenger设置指南.md)
- [设置Messenger问候语指南](../guides/设置Messenger问候语指南.md)
- [Facebook Webhook事件说明](../guides/FACEBOOK_WEBHOOK_EVENTS.md)
- [页面自动回复控制指南](../guides/页面自动回复控制指南.md)

## 🆘 故障排除

### 问题1：Greeting Message未显示

**可能原因**：
- Facebook页面未设置Greeting Message
- Greeting Message超过160字符限制

**解决方法**：
1. 检查Facebook页面设置
2. 确保文案不超过160字符
3. 使用脚本 `scripts/tools/setup_three_step_messenger.py` 自动设置

### 问题2：用户回复未触发路由判断

**可能原因**：
- 路由规则配置不正确
- YES关键词识别失败

**解决方法**：
1. 检查 `routing_rules` 配置
2. 查看日志确认用户回复内容
3. 测试不同的回复格式

### 问题3：Telegram链接无法访问

**可能原因**：
- Telegram群组链接错误
- 群组设置为私有

**解决方法**：
1. 验证Telegram群组链接是否正确
2. 确保群组设置为公开或已正确配置邀请链接
3. 测试链接是否可以直接访问

## 📞 支持

如有问题，请：

1. 查看相关文档
2. 检查日志文件 `logs/app.log`
3. 联系技术支持

---

**最后更新**：2025-12-21  
**版本**：1.0.0  
**合规状态**：✅ 符合Meta Non-Deceptive Policy

